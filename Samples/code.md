# Code

Here is some `inline code`.

## Fenced Code Block

```swift
import SwiftUI

struct Example: View {
    var body: some View {
        Text("Hello, world!")
    }
}
```

```javascript
function greet(name) {
    console.log(`Hello, ${name}!`);
}
greet("DigBick");
```
